spring:
  datasource:
    username: tdq_app
    password: pA_tdqap_22
    url: jdbc:mysql://${MYSQL_HOST:mysqltdq.db.dev.ebayc3.com}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:tdq}?characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
  flyway:
    enabled: true
    out-of-order: true

logging:
  level:
    com.ebay.dap.epic.tdq: debug
    org.springframework.security: debug
    com.baomidou.mybatisplus: DEBUG

global:
  web:
    security:
      enabled: true


pronto:
  scheme: https
  hostname: estess762test-dataslc.stratus.qa.ebay.com
  port: 443
  api-key: _TDQ_SVC_USER
  api-value: anGp2222anGp2222$